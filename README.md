
# NodeJS API Gateway

Project built with docker and NodeJS to mimic an API Gateway. Should handle authorization, authentication and rate limiting.

1. Configure your API KEYS in your .env  
2. Run 
```http
  docker compose up
```
